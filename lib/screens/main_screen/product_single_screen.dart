

import 'package:flutter/material.dart';

class PruductSingleScreen extends StatelessWidget {
  const PruductSingleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
      width: double.infinity,
      height: double.infinity,
      child: Text(" صفحه جزئیات محصول",style: TextStyle(fontSize: 30),),
    );
  }
}